app.factory("contactList", function() {
  var ContactList = {};

  ContactList.contactList = [];

  ContactList.addContact = function(obj) {
    ContactList.contactList.push(obj);
  };

  ConactList.findContact = function(name) {
    //todo
  };

  ContactList.removeContact = function(index) {
    //todo
  };
});
